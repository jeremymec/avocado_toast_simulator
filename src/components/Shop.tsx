import * as React from "react";
import * as CSS from 'csstype';

interface ShopProps {

}

const Shop = (props: ShopProps) => {

  return (
    <div>
        <h1>Shop</h1>
    </div>
  );
};

export default Shop;